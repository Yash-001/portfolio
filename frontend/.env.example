# ── App ───────────────────────────────────────────────────────────────────
VITE_APP_TITLE=Yash Ranjan | Enterprise Software & AI Consultant
VITE_APP_URL=https://yourdomain.com
VITE_APP_EMAIL=yash.ranjan.dev@proton.me
VITE_APP_PHONE=+91-0000000000

# ── EmailJS (https://www.emailjs.com) ─────────────────────────────────────
# 1. Create a free account at emailjs.com
# 2. Add an Email Service (Gmail, Outlook, etc.) → copy the Service ID
# 3. Create TWO Email Templates:
#    a) Admin notification    → copy as VITE_EMAILJS_TEMPLATE_ID
#    b) Auto-reply to visitor → copy as VITE_EMAILJS_AUTOREPLY_TEMPLATE_ID
# 4. Go to Account → API Keys → copy the Public Key
#
# Admin template variables:
#   {{from_name}}       — sender's full name
#   {{from_email}}      — sender's email address
#   {{reply_to}}        — reply-to (same as from_email)
#   {{project_type}}    — selected project type
#   {{budget}}          — selected budget range
#   {{message}}         — message body
#   {{submitted_at}}    — submission timestamp (IST)
#   {{attachment_name}} — attached filename (optional)
#   {{attachment_data}} — base64 data URL (optional)
#   {{site_url}}        — your portfolio URL
#
# Auto-reply template variables:
#   {{to_name}}         — visitor's name
#   {{to_email}}        — visitor's email (set as To address)
#   {{reply_to}}        — your email address
#   {{owner_name}}      — your name
#   {{response_time}}   — e.g. "24 hours"
#   {{project_type}}    — what they selected
#   {{site_url}}        — your portfolio URL
#   {{calendly_url}}    — your Calendly link
VITE_EMAILJS_SERVICE_ID=service_xxxxxxx
VITE_EMAILJS_TEMPLATE_ID=template_xxxxxxx
VITE_EMAILJS_AUTOREPLY_TEMPLATE_ID=template_xxxxxxx
VITE_EMAILJS_PUBLIC_KEY=xxxxxxxxxxxxxxxxxxxx

# ── Calendly ──────────────────────────────────────────────────────────────
# Your Calendly scheduling link (e.g. https://calendly.com/yourname/30min)
VITE_CALENDLY_URL=https://calendly.com/yourname/30min

# ── Analytics ─────────────────────────────────────────────────────────────
# Google Analytics 4 — https://analytics.google.com
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Plausible Analytics — https://plausible.io (use your site domain, e.g. yourdomain.com)
VITE_PLAUSIBLE_DOMAIN=yourdomain.com

# Microsoft Clarity — https://clarity.microsoft.com (Project ID from Settings > Overview)
VITE_CLARITY_PROJECT_ID=xxxxxxxxxx

# Set to "true" to enable analytics in local dev (default: off in dev)
VITE_ANALYTICS_DEV=false

# ── Blog API (optional) ───────────────────────────────────────────────────
VITE_BLOG_API_URL=https://api.yourdomain.com

# ── AI Service ───────────────────────────────────────────────────────────────
# URL of the ai-service FastAPI server (ai-service/app/main.py)
# In development: http://localhost:8000
# In production: https://ai.yourdomain.com
VITE_AI_SERVICE_URL=http://localhost:8000
